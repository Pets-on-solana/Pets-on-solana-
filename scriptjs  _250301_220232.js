document.getElementById('pet-form').addEventListener('submit', function(e) { e.preventDefault(); const petName = document.getElementById('pet-name').value; const petPhoto = document.getElementById('pet-photo').files[0]; const wallet = document.getElementById('wallet').value; const tokenAddress = document.getElementById('token-address').value; console.log(`New Pet Submission: Name: ${petName} Photo: ${petPhoto.name} Wallet: ${wallet} Token: ${tokenAddress}`); alert('Pet beamed up! We’ll add it to the galaxy soon!'); this.reset(); });